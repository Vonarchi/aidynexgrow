import { EnginePlaceholderPage } from '../../../components/engine/EnginePlaceholderPage'

export default function AdminDomainsPage() {
  return (
    <EnginePlaceholderPage
      eyebrow="Admin"
      title="Domains"
      description="Domain verification, primary domain selection, SSL status, and DNS launch steps will be managed here."
    />
  )
}
