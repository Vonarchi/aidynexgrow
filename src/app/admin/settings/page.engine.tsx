import { EnginePlaceholderPage } from '../../../components/engine/EnginePlaceholderPage'

export default function AdminSettingsPage() {
  return (
    <EnginePlaceholderPage
      eyebrow="Admin"
      title="Engine settings"
      description="Feature flags, storage settings, analytics connections, and publication safeguards will be configured here."
    />
  )
}
