import { EnginePlaceholderPage } from '../../../components/engine/EnginePlaceholderPage'

export default function AdminTemplatesPage() {
  return (
    <EnginePlaceholderPage
      eyebrow="Admin"
      title="Templates"
      description="Template library management will cover versions, industry defaults, section schemas, preview URLs, and deprecation status."
    />
  )
}
